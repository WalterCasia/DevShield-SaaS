# Plantilla de secretos para Streamlit Community Cloud.
#
# NO copies este archivo a `secrets.toml` en tu repositorio: el archivo real
# está en .gitignore. En el despliegue, pega este contenido en
# Streamlit Cloud → tu app → Settings → Secrets.
#
# Para desarrollo local puedes usar .env en lugar de este archivo.

GOOGLE_API_KEY = "AIzaSy...pega-aqui-tu-clave"
